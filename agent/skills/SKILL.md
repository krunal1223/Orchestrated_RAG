---
name: rag-ui
description: RAG frontend UI builder. Trigger on any UI or frontend request.
---

Build a single index.html file (HTML+CSS+JS, no frameworks).

Project: RAG app with Qdrant vector DB, pipeline.py retrieval, query.py interface.

Design: bg #050A14, accent #F5A623, text #E8E0D0. Fonts: Syne + IBM Plex Mono from Google Fonts. Inline SVG icons only — no libraries.

Build: sidebar (history) + chat (messages + input) + sources panel (doc chunks with relevance scores).

Mock data only. One complete file, no placeholders.
